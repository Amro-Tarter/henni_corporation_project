import React from 'react';

export const TooltipProvider = ({ children }) => {
  return <>{children}</>;
};

// Placeholder tooltip provider.
// Later you can replace it with something like radix-ui Tooltip or your own custom tooltip logic.
